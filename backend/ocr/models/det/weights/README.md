Pretrained weight placeholder for `resnet50_imagenet.pdparams`.

The current workspace is offline, so the actual pretrained weight was not downloaded automatically.
Place the ImageNet pretrained Paddle weight here before training.
