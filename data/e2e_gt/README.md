# E2E 정답 매트릭스 위치

OCR-E03 평가에서 사용하는 근무표 단위 정답 JSON을 이 폴더에 둡니다.

파일명은 원본 이미지 stem과 동일하게 맞춥니다.

예시: `paper_hospital_0001.json`

각 JSON은 `image_path`, `format`, `industry`, `year_month`, `workers`, `schedule`, `metadata` 필드를 포함해야 합니다.
